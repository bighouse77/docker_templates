version: "3.9"

services:
  app:
    container_name: vue_generator
    build: .
    working_dir: /var/www/html
    volumes:
      - ${PROJECT_PATH:-../../vue-app}:/var/www/html
    ports:
      - "5173:5173"
    environment:
      - USER_ID=${UID:-1000}
      - GROUP_ID=${GID:-1000}
